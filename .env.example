# Only required when refreshing data from API-Football.
API_FOOTBALL_KEY=replace_with_your_api_football_key

# Comma-separated origins allowed to call the FastAPI service.
FRONTEND_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
